数组指针本质是指针
int (*p2)[10]; 定义了一个指向类型为int [10]的数组指针变量

char a[5]={'a','b','c','d'};
1.char (*p1)[5]= &a;
2.char (*p1)[5]= a;
数组指针p1指向数组a的地址，所以p1是一个二级指针
注意：第二种赋值方式是错误的,因为指针指向类型不同

操作：类似二维数组的行指针
printf("p1=%c\n",**p1);
printf("p1+1=%c\n",**(p1+1));